#include <stdio.h>
	
	int main (){
		float soma;
		float media;
		int idade;
		for(int i=0;i<=20;i++){
		    printf("idade:\n ");
		    scanf("%d",&idade);
		    soma=soma+idade;
		}
		    media=soma/20;
		printf("media eh %2.f",media);
	    
	}
